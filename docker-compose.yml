version: "3.9"

services:

  backend:

    build: ./backend

    ports:
      - "8000:8000"

    container_name: efip-backend

  frontend:

    build: ./frontend

    ports:
      - "5173:5173"

    depends_on:
      - backend

    container_name: efip-frontend